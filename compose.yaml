services:
  app:
    image: node:24-alpine
    working_dir: /app
    volumes:
      - .:/app:rw
      - node_modules:/app/node_modules
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"

volumes:
  node_modules:
